#include <stdio.h>
main()
{
    int ch,a,z;
    printf("enter the value /n");
    scanf("%d",&ch);
    if(a>=ch && ch<=z){
    printf("%d is alphabet ",ch);
  }
   else{
    printf("%d is number",ch);
}
}

