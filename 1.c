#include <stdio.h>
int main() {
    int a,b;
  printf("Enter the value :");
  scanf("%d%d",&a,&b);
  if(a>b)
  {
  printf("%dis greater",a);
  }
  else
  {
  printf("%dis greater",b);
  }
    return 0;
}